# ========================================
# INSTAGRAM SCRAPER FRONTEND ENVIRONMENT VARIABLES
# ========================================

# Development Configuration
NEXT_PUBLIC_API_URL=http://localhost:5002/api
NEXT_PUBLIC_WS_URL=http://localhost:5002

# Production Configuration (examples)
# NEXT_PUBLIC_API_URL=https://your-scraper-backend.railway.app/api
# NEXT_PUBLIC_WS_URL=https://your-scraper-backend.railway.app

# ========================================
# NOTES:
# 1. API_URL points to the SCRAPER backend (port 5002), not analyzer
# 2. WS_URL is for WebSocket connections (real-time updates)
# 3. Both URLs should point to the same backend server
# 4. In production, use HTTPS URLs
# 5. The scraper backend handles all frontend requests
# ========================================